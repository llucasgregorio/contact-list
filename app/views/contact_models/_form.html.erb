<%= form_with(model: contact_model, local: true) do |form| %>
  <% if contact_model.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(contact_model.errors.count, "error") %> prohibited this contact_model from being saved:</h2>

      <ul>
      <% contact_model.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :telefone %>
    <%= form.text_field :telefone %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
